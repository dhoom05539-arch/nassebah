# حزمة نظام بصمة نسيبة

## التشغيل المحلي

1. ثبّت Node.js 22 أو أحدث و`pnpm`.
2. نفّذ `pnpm install`.
3. جهّز متغيرات البيئة المطلوبة من منصة WebDev، خصوصًا `JWT_SECRET` وأسرار تيليجرام والمصادقة.
4. نفّذ `pnpm check` ثم `pnpm build`.
5. للتشغيل التطويري استخدم `pnpm dev`، وللتشغيل الإنتاجي بعد البناء استخدم `pnpm start`.

## النشر على Railway

المشروع مجهز بملفات `Dockerfile` و`railway.json` ويستخدم **ملف JSON بدل MySQL**. لذلك لا يكفي إنشاء متغير `DATABASE_URL`؛ هذا الإصدار لا يقرأه لتخزين الحضور.

### 1. إعداد التخزين الدائم

أنشئ **Volume** داخل خدمة Railway واربطه بالمسار:

```text
/data
```

واترك `JSON_DB_PATH` على قيمته الافتراضية `/data/attendance.json`. إذا لم يوجد الملف عند أول تشغيل، سيُنسخ `data/attendance.seed.json` تلقائيًا إلى الـ Volume. لا تحذف الـ Volume لأنه يحتوي جميع سجلات الحضور.

إذا لم تُنشئ Volume، قد يعمل التطبيق مؤقتًا، لكن ملف الحضور سيُحذف عند إعادة النشر أو نقل الخدمة إلى نسخة جديدة.

### 2. متغيرات البيئة المطلوبة

أضف في **Variables** داخل Railway:

```text
JWT_SECRET=<قيمة عشوائية طويلة>
DASHBOARD_PASSWORD_HASH=<الهاش الخاص بكلمة المرور>
OWNER_OPEN_ID=<معرّف المالك إذا استُخدمت مصادقة Manus>
```

أضف متغيرات Telegram فقط إذا كانت التكاملات مفعّلة، مثل `TELEGRAM_BOT_TOKEN` و`TELEGRAM_ADMIN_CHAT_ID`.

`DASHBOARD_PASSWORD_HASH` ليس كلمة المرور نفسها، بل قيمة بصيغة `salt:hash` التي تنتجها الدالة `hashPassword` في `server/local-auth.ts`. لا تضع كلمة المرور أو الأسرار داخل Git أو ملف ZIP. إذا لم تضبط هذا المتغير فلن ينجح أول تسجيل دخول، لأن بيانات الاعتماد تُحفظ في `/data/attendance.json` بعد أول دخول ناجح فقط.

لتوليد القيمة محليًا، وبعد تثبيت الحزم، نفّذ الأمر التالي ثم انسخ الناتج إلى Railway:

```bash
pnpm auth:hash "كلمة المرور القوية"
```

لا تضع الناتج في `package.json` أو داخل ملفات المشروع.

### 3. بعد النشر

اختبر:

```text
https://<your-domain>/api/health
```

ويجب أن يعيد:

```json
{"ok":true,"storage":"json"}
```

ثم سجّل الدخول باسم المستخدم الافتراضي `u85ll` وكلمة المرور المطابقة للهاش الذي ضبطته. بعد الدخول الأول يمكن تغيير بيانات الدخول من لوحة التحكم، وسيُحفظ التغيير داخل ملف JSON الدائم.

إذا ظهرت رسالة تفيد بعدم الوصول إلى قاعدة البيانات، فتحقق من الآتي:

1. أن Volume مربوط فعلًا بـ `/data` وليس بمسار آخر.
2. أن `JWT_SECRET` و`DASHBOARD_PASSWORD_HASH` موجودان في **نفس خدمة Railway** التي تشغّل التطبيق.
3. أن سجلات النشر لا تحتوي على `ENOENT` لملف `patches/wouter@3.7.1.patch`.
4. أن الطلبات تصل إلى نفس النطاق المنشور، وليس إلى Preview قديم.

## رفع جدول الموظفين

من لوحة التحكم افتح بطاقة **رفع جدول الموظفين**، ثم اختر ملف Excel (`.xlsx` أو `.xls`) أو CSV. يجب أن يحتوي الصف الأول على عناوين الأعمدة التالية أو ما يقابلها بالعربية:

| الحقل | أمثلة لأسماء الأعمدة المقبولة |
| --- | --- |
| الاسم | `الاسم`، `اسم الموظف`، `name`، `employee name` |
| الفرع | `الفرع`، `branch` |
| الجوال | `الجوال`، `الهاتف`، `phone`، `mobile` |
| بداية الدوام | `بداية الدوام`، `وقت الحضور`، `start time` |
| نهاية الدوام | `نهاية الدوام`، `وقت الانصراف`، `end time` |

تُعرض معاينة قبل الحفظ. لا تُحذف سجلات الحضور السابقة. إذا تطابق الاسم والفرع مع موظف موجود، يتم تحديث بيانات الجوال والدوام بدل إنشاء نسخة مكررة. الفروع غير الموجودة أو غير النشطة تُرفض مع توضيح أسمائها.

وقت بداية الدوام يُستخدم لتحديد جدول الأحد إلى الخميس في دورة الاستيراد الحالية، والجمعة والسبت إجازة افتراضية. يمكن تعديل الجدول لاحقًا حسب سياسة الشركة.

## التحقق

- `pnpm test`
- `pnpm check`
- `pnpm build`

لا توجد أسرار أو ملفات `node_modules` داخل الحزمة المضغوطة؛ تُضبط الأسرار في بيئة النشر فقط.

ملاحظة مهمة: تم إصلاح `Dockerfile` لنسخ مجلد `patches` قبل أوامر `pnpm install` في مرحلتي البناء والتشغيل. يجب رفع التعديلات الجديدة كاملة، بما فيها `Dockerfile` ومجلد `patches`.
