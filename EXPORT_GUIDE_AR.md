# حزمة نظام بصمة نسيبة

## التشغيل المحلي

1. ثبّت Node.js 22 أو أحدث و`pnpm`.
2. نفّذ `pnpm install`.
3. جهّز متغيرات البيئة المطلوبة من منصة WebDev، خصوصًا `DATABASE_URL` و`JWT_SECRET` وأسرار تيليجرام والمصادقة.
4. نفّذ `pnpm check` ثم `pnpm build`.
5. للتشغيل التطويري استخدم `pnpm dev`، وللتشغيل الإنتاجي بعد البناء استخدم `pnpm start`.

## النشر على Railway

المشروع مجهز بملفات `Dockerfile` و`railway.json` ويستخدم ملف JSON بدل MySQL. أنشئ Volume دائمًا في Railway واربطه بالمسار `/data`، ثم اترك `JSON_DB_PATH` على قيمته الافتراضية `/data/attendance.json`. إذا لم يوجد الملف عند أول تشغيل، سيُنسخ `data/attendance.seed.json` تلقائيًا إلى الـ Volume. لا تحذف الـ Volume لأنه يحتوي جميع سجلات الحضور.

أضف متغيرات البيئة المطلوبة للتشغيل، مثل `JWT_SECRET` و`DASHBOARD_PASSWORD_HASH` و`OWNER_OPEN_ID` و`TELEGRAM_BOT_TOKEN` و`TELEGRAM_ADMIN_CHAT_ID` حسب التكاملات المستخدمة. لا تضع هذه القيم داخل Git أو ملف ZIP.

يتضمن ملف seed الفروع والموظفين وسجلات الحضور غير الحساسة التي كانت موجودة عند تجهيز النسخة. لا يتضمن بيانات دخول المالك أو رموز Telegram؛ عند أول تسجيل دخول ناجح تُحفظ بيانات اعتماد لوحة التحكم داخل ملف JSON الدائم.

يوفر الخادم مسار فحص صحة: `/api/health`. ينشئ المخزن نسخة احتياطية تلقائية في `/data/attendance.json.bak` قبل كل كتابة ناجحة، ويستخدم سلسلة قفل لمنع تعارض عمليات الحضور المتزامنة داخل النسخة الواحدة.

## رفع جدول الموظفين

من لوحة التحكم افتح بطاقة **رفع جدول الموظفين**، ثم اختر ملف Excel (`.xlsx` أو `.xls`) أو CSV. يجب أن يحتوي الصف الأول على عناوين الأعمدة التالية أو ما يقابلها بالعربية:

| الحقل | أمثلة لأسماء الأعمدة المقبولة |
| --- | --- |
| الاسم | `الاسم`، `اسم الموظف`، `name`، `employee name` |
| الفرع | `الفرع`، `branch` |
| الجوال | `الجوال`، `الهاتف`، `phone`، `mobile` |
| بداية الدوام | `بداية الدوام`، `وقت الحضور`، `start time` |
| نهاية الدوام | `نهاية الدوام`، `وقت الانصراف`، `end time` |

تُعرض معاينة قبل الحفظ. لا تُحذف سجلات الحضور السابقة. إذا تطابق الاسم والفرع مع موظف موجود، يتم تحديث بيانات الجوال والدوام بدل إنشاء نسخة مكررة. الفروع غير الموجودة أو غير النشطة تُرفض مع توضيح أسمائها.

وقت بداية الدوام يُستخدم لتحديد جدول الأحد إلى الخميس في دورة الاستيراد الحالية، والجمعة والسبت إجازة افتراضية. يمكن تعديل الجدول لاحقًا حسب سياسة الشركة.

## التحقق

- `pnpm test`
- `pnpm check`
- `pnpm build`

لا توجد أسرار أو ملفات `node_modules` داخل الحزمة المضغوطة؛ تُضبط الأسرار في بيئة النشر فقط.
